{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install -g tsx && node scripts/prisma-prod.mjs emit && npm install",
    "watchPaths": ["apps/api", "apps/web", "packages/shared"]
  },
  "deploy": {
    "startCommand": "node scripts/prisma-prod.mjs push && node --import tsx apps/api/src/index.ts",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 600,
    "restartPolicyType": "ON_FAILURE",
    "sleepApplication": 30000
  }
}